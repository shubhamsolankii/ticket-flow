# Copy this file to .env and fill in values for local development.
# .env is gitignored. This file is committed — no real credentials here.

# PostgreSQL
POSTGRES_USER=auth_user
POSTGRES_PASSWORD=change_me
POSTGRES_DB=auth_db

# Redis
REDIS_PASSWORD=change_me

# Kafka — no credentials needed for local dev (no auth configured)
# Kafka is accessed at localhost:9092 from the host machine